{
  "buildCommand": "python3 scripts/apply_global_compliance.py && python3 scripts/fix_global_compliance_output.py && tsc --noEmit && vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
