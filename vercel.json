{
  "buildCommand": "python3 scripts/apply_global_compliance.py && tsc --noEmit && vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
